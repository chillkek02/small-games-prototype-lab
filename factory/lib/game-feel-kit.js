import fsp from 'node:fs/promises';
import path from 'node:path';

export const GAME_FEEL_VERSION='1.0.1';

function css(){return `.ggf-transition{position:fixed;z-index:1600;inset:0;pointer-events:none;opacity:0;background:#fff}.ggf-joystick{position:fixed;z-index:700;left:calc(18px + env(safe-area-inset-left,0px));bottom:calc(18px + env(safe-area-inset-bottom,0px));width:116px;height:116px;border-radius:50%;background:rgba(16,27,41,.22);border:1px solid rgba(255,255,255,.18);backdrop-filter:blur(5px);touch-action:none;user-select:none}.ggf-joystick.hidden{display:none!important}.ggf-joystick-knob{position:absolute;left:50%;top:50%;width:54px;height:54px;margin:-27px;border-radius:50%;background:rgba(255,255,255,.86);box-shadow:0 8px 22px rgba(0,0,0,.22);transform:translate(0,0)}@media(pointer:fine){.ggf-joystick.auto-hide{display:none}}`}

function js(){return `(()=>{'use strict';
const VERSION=${JSON.stringify('1.0.1')};
let audioCtx=null,master=.55;
function context(){if(audioCtx)return audioCtx;const C=window.AudioContext||window.webkitAudioContext;if(!C)return null;audioCtx=new C();return audioCtx}
function unlock(){const c=context();if(c&&c.state==='suspended')c.resume().catch(()=>{})}
addEventListener('pointerdown',unlock,{once:true,passive:true});
function tone({freq=440,endFreq=freq,type='sine',duration=.08,gain=.09,delay=0}={}){const c=context();if(!c)return;const t=c.currentTime+delay,o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(freq,t);o.frequency.exponentialRampToValueAtTime(Math.max(30,endFreq),t+duration);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(Math.max(.0001,gain*master),t+.008);g.gain.exponentialRampToValueAtTime(.0001,t+duration);o.connect(g).connect(c.destination);o.start(t);o.stop(t+duration+.02)}
function noise({duration=.08,gain=.05,highpass=600}={}){const c=context();if(!c)return;const frames=Math.max(1,Math.floor(c.sampleRate*duration)),buf=c.createBuffer(1,frames,c.sampleRate),d=buf.getChannelData(0);for(let i=0;i<frames;i++)d[i]=Math.random()*2-1;const src=c.createBufferSource(),filter=c.createBiquadFilter(),g=c.createGain();src.buffer=buf;filter.type='highpass';filter.frequency.value=highpass;g.gain.setValueAtTime(gain*master,c.currentTime);g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+duration);src.connect(filter).connect(g).connect(c.destination);src.start()}
const sfx={click(){tone({freq:420,endFreq:520,duration:.05,gain:.055,type:'triangle'})},collect(){tone({freq:620,endFreq:980,duration:.09,gain:.075,type:'sine'});tone({freq:900,endFreq:1320,duration:.08,gain:.045,delay:.055})},hit(){tone({freq:150,endFreq:72,duration:.12,gain:.095,type:'square'});noise({duration:.06,gain:.035,highpass:900})},whoosh(){noise({duration:.13,gain:.035,highpass:1600});tone({freq:260,endFreq:520,duration:.11,gain:.025,type:'triangle'})},success(){tone({freq:523,endFreq:659,duration:.11,gain:.07});tone({freq:659,endFreq:784,duration:.12,gain:.065,delay:.10});tone({freq:784,endFreq:1047,duration:.16,gain:.075,delay:.21})},fail(){tone({freq:330,endFreq:210,duration:.16,gain:.07,type:'triangle'});tone({freq:240,endFreq:120,duration:.20,gain:.06,delay:.12})},setVolume(v){master=Math.max(0,Math.min(1,Number(v)||0))},mute(){master=0}};
function transitionLayer(){let e=document.querySelector('.ggf-transition');if(!e){e=document.createElement('div');e.className='ggf-transition';document.body.append(e)}return e}
function flash({color='#ffffff',duration=160,peak=.55}={}){const e=transitionLayer();e.style.background=color;e.animate([{opacity:0},{opacity:peak},{opacity:0}],{duration,easing:'ease-out'}).finished.catch(()=>{})}
function wipe({color='#0c1420',duration=360,onCovered=null}={}){const e=transitionLayer();e.style.background=color;const a=e.animate([{transform:'translateX(-102%)',opacity:1},{transform:'translateX(0)',opacity:1},{transform:'translateX(0)',opacity:1},{transform:'translateX(102%)',opacity:1}],{duration,easing:'cubic-bezier(.65,0,.35,1)'});setTimeout(()=>{try{onCovered?.()}catch{}},duration*.48);a.finished.finally(()=>{e.style.opacity=0;e.style.transform=''})}
function punch(element,{scale=1.12,duration=180}={}){if(!element?.animate)return;element.animate([{transform:'scale(1)'},{transform:'scale('+scale+')',offset:.42},{transform:'scale(1)'}],{duration,easing:'cubic-bezier(.2,.8,.25,1)'})}
function createJoystick({autoHide=true,onChange=null,radius=42}={}){const base=document.createElement('div'),knob=document.createElement('div');base.className='ggf-joystick'+(autoHide?' auto-hide':'');knob.className='ggf-joystick-knob';base.append(knob);document.body.append(base);let active=null,value={x:0,y:0};const emit=()=>{try{onChange?.(value)}catch{}};function set(clientX,clientY){const r=base.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=clientX-cx,dy=clientY-cy,len=Math.hypot(dx,dy)||1,s=Math.min(radius,len)/len;value={x:dx*s/radius,y:dy*s/radius};knob.style.transform='translate('+(dx*s)+'px,'+(dy*s)+'px)';emit()}function reset(){value={x:0,y:0};knob.style.transform='translate(0,0)';emit()}base.addEventListener('pointerdown',e=>{active=e.pointerId;base.setPointerCapture?.(active);set(e.clientX,e.clientY)});base.addEventListener('pointermove',e=>{if(e.pointerId===active)set(e.clientX,e.clientY)});const up=e=>{if(e.pointerId===active){active=null;reset()}};base.addEventListener('pointerup',up);base.addEventListener('pointercancel',up);return{element:base,get value(){return value},destroy:()=>base.remove(),show:()=>base.classList.remove('hidden'),hide:()=>{base.classList.add('hidden');reset()},setAutoHide:v=>base.classList.toggle('auto-hide',Boolean(v))}}
function objectPool({create,reset,max=64}={}){const free=[],used=new Set();return{acquire(data){let o=free.pop()||create?.();if(!o)return null;used.add(o);reset?.(o,data,true);return o},release(o){if(!used.has(o))return;used.delete(o);reset?.(o,null,false);if(free.length<max)free.push(o)},releaseAll(){for(const o of[...used])this.release(o)},stats(){return{active:used.size,free:free.length,max}}}}
function threeFollow(camera,target,{offset={x:0,y:5,z:7},lookAhead={x:0,y:0,z:0},positionDamp=7,lookDamp=9}={}){const THREE=window.THREE;if(!THREE||!camera||!target)return()=>{};const desired=new THREE.Vector3(),look=new THREE.Vector3(),currentLook=new THREE.Vector3(),offsetV=new THREE.Vector3(offset.x||0,offset.y||0,offset.z||0);target.getWorldPosition?.(currentLook);return function(dt=.016,velocity=null){const a=1-Math.exp(-positionDamp*Math.min(.05,dt)),b=1-Math.exp(-lookDamp*Math.min(.05,dt));target.getWorldPosition?.(look);desired.copy(look).add(offsetV);if(velocity){desired.x+=(velocity.x||0)*(lookAhead.x||0);desired.y+=(velocity.y||0)*(lookAhead.y||0);desired.z+=(velocity.z||0)*(lookAhead.z||0)}camera.position.lerp(desired,a);currentLook.lerp(look,b);camera.lookAt(currentLook)}}
function threeShake(camera,{duration=.18,strength=.08}={}){if(!camera)return;const start=performance.now(),base=camera.position.clone();function tick(now){const t=(now-start)/1000,p=Math.min(1,t/duration),fade=1-p;camera.position.set(base.x+(Math.random()-.5)*strength*fade,base.y+(Math.random()-.5)*strength*fade,base.z+(Math.random()-.5)*strength*fade);if(p<1)requestAnimationFrame(tick);else camera.position.copy(base)}requestAnimationFrame(tick)}
function threeZoomPunch(camera,{amount=.9,duration=180}={}){if(!camera)return;const start=performance.now(),base=camera.fov||50;function tick(now){const p=Math.min(1,(now-start)/duration),wave=Math.sin(p*Math.PI);camera.fov=base-amount*wave;camera.updateProjectionMatrix?.();if(p<1)requestAnimationFrame(tick);else{camera.fov=base;camera.updateProjectionMatrix?.()}}requestAnimationFrame(tick)}
function phaserImpact(scene,target,{scale=1.10,shake=.004,duration=90}={}){scene?.tweens?.add?.({targets:target,scaleX:scale,scaleY:scale,duration,yoyo:true,ease:'Back.Out'});scene?.cameras?.main?.shake?.(duration,shake)}
window.GutpopperFeel={version:VERSION,sfx,transition:{flash,wipe,punch},input:{createJoystick},pool:{create:objectPool},three:{follow:threeFollow,shake:threeShake,zoomPunch:threeZoomPunch},phaser:{impact:phaserImpact}};window.dispatchEvent(new CustomEvent('gutpopper-game-feel-ready',{detail:{version:VERSION}}));})();\n`}

export async function writeGameFeelKit({gameDir}){const starter=path.join(gameDir,'starter');await fsp.mkdir(starter,{recursive:true});const manifest={name:'Gutpopper Game Feel Forge',version:GAME_FEEL_VERSION,systems:['synth-sfx','transitions','joystick','object-pool','three-camera-follow','three-shake','three-zoom-punch','phaser-impact'],principle:'Reuse proven feel systems; spend game-specific work on timing and personality.'};await Promise.all([fsp.writeFile(path.join(starter,'game-feel.css'),css(),'utf8'),fsp.writeFile(path.join(starter,'game-feel.js'),js(),'utf8'),fsp.writeFile(path.join(starter,'game-feel.json'),JSON.stringify(manifest,null,2),'utf8')]);return manifest}

export function gameFeelBrief(){return `GUTPOPPER GAME FEEL FORGE v${GAME_FEEL_VERSION}\n- ./starter/game-feel.js exposes window.GutpopperFeel; ./starter/game-feel.css contains the optional touch joystick and transition layer.\n- Reuse the Forge for tiny synthesized SFX, camera follow/damping, impact shake/zoom punch, tactile transitions, mobile joystick and object pooling instead of rebuilding them per game.\n- Sound is generated through WebAudio and adds no audio download. Keep it subtle; unlock only after player input and mute/pause it during ads when audio is active.\n- Camera response should clarify impact and motion, not make players sick. Use small, short shake/punch values on phone.\n- Controls must remain obvious and responsive; never hide core controls behind decoration.\n- Object pooling is preferred for repeated particles/projectiles/collectibles.\n- Game-specific timing and thematic feedback still matter: a cleaning spray, goblin hit, fruit merge and vehicle collision should not all feel identical.`}
