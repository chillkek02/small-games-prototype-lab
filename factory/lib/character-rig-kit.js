import fsp from 'node:fs/promises';
import path from 'node:path';

export const CHARACTER_FORGE_VERSION='1.0.0';

function runtime(){return `(()=>{'use strict';
const VERSION=${JSON.stringify(CHARACTER_FORGE_VERSION)};const T=()=>window.THREE;
function mat(color,rough=.66,metal=.02){const THREE=T();if(!THREE)return null;return window.GutpopperShaderFX?.three?.toyPlastic?.(color,{roughness:rough,metalness:metal,rimStrength:.08,gradientStrength:.05})||new THREE.MeshStandardMaterial({color,roughness:rough,metalness:metal})}
function mesh(g,m){const THREE=T();if(!THREE)return null;const x=new THREE.Mesh(g,m);x.castShadow=true;x.receiveShadow=true;return x}
function group(){const THREE=T();return THREE?new THREE.Group():null}function add(p,...c){c.flat().filter(Boolean).forEach(x=>p?.add?.(x));return p}function pos(o,x=0,y=0,z=0){o?.position?.set(x,y,z);return o}function box(w,h,d,c){const THREE=T();return THREE?mesh(new THREE.BoxGeometry(w,h,d),mat(c)):null}function sph(r,c,ws=14,hs=9){const THREE=T();return THREE?mesh(new THREE.SphereGeometry(r,ws,hs),mat(c)):null}function cyl(r1,r2,h,c,s=10){const THREE=T();return THREE?mesh(new THREE.CylinderGeometry(r1,r2,h,s),mat(c)):null}function cone(r,h,c,s=10){const THREE=T();return THREE?mesh(new THREE.ConeGeometry(r,h,s),mat(c)):null}
function defaults(archetype){const map={raccoon:{body:0x6f7780,belly:0xc7ccd0,accent:0x323840,skin:0xf0c49a},cat:{body:0xf19a59,belly:0xffdfbf,accent:0x5b4438,skin:0xf0c49a},capybara:{body:0xa87952,belly:0xd7b38d,accent:0x4b372d,skin:0xf0c49a},penguin:{body:0x303947,belly:0xf5f5ef,accent:0xf2a23d,skin:0xf0c49a},frog:{body:0x68c66b,belly:0xb8e58e,accent:0x294e36,skin:0xf0c49a},goblin:{body:0x76b65b,belly:0xa3cf75,accent:0x3d4b31,skin:0x76b65b},slime:{body:0x63d6ae,belly:0x9cf2d1,accent:0x244b57,skin:0x63d6ae},fruit:{body:0xff6b67,belly:0xffa197,accent:0x3a7544,skin:0xff6b67},robot:{body:0x65aee9,belly:0xb9ddf7,accent:0x334a63,skin:0x65aee9},humanoid:{body:0x5ca8ef,belly:0xffffff,accent:0x33465d,skin:0xf0c49a}};return map[archetype]||map.humanoid}
function earPair(parts,color,{round=false,long=false}={}){const THREE=T();if(!THREE)return;for(const side of[-1,1]){const e=round?sph(long?.22:.19,color,10,7):cone(long?.18:.20,long?.55:.38,color,9);e.position.set(side*.24,.26,0);if(!round)e.rotation.z=side*.08;parts.head.add(e);if(!parts.ears)parts.ears=[];parts.ears.push(e)}}
function face(parts,palette,{snout=false,beak=false,mask=false}={}){const eyeM=mat(0x1d2630,.5,0);for(const side of[-1,1]){const e=sph(.055,0xffffff,10,7),p=sph(.027,0x17212c,8,6);e.position.set(side*.105,.055,.285);p.position.set(side*.105,.055,.335);parts.head.add(e,p)}if(mask){for(const side of[-1,1]){const m=sph(.12,palette.accent,10,7);m.scale.set(1.18,.72,.24);m.position.set(side*.095,.055,.265);parts.head.add(m)}}if(beak){const b=cone(.105,.22,palette.accent,8);b.rotation.x=Math.PI/2;b.position.set(0,-.025,.34);parts.head.add(b)}else if(snout){const s=sph(.14,palette.belly,12,8);s.scale.set(1,.72,.58);s.position.set(0,-.08,.285);parts.head.add(s);const n=sph(.055,palette.accent,8,6);n.position.set(0,-.065,.375);parts.head.add(n)}else{const mouth=box(.10,.025,.018,palette.accent);mouth.position.set(0,-.085,.323);parts.head.add(mouth)}}
function addTail(parts,archetype,palette){const THREE=T();if(!THREE)return;let tail=null;if(archetype==='raccoon'||archetype==='cat'){tail=cyl(.075,.12,.72,palette.body,10);tail.rotation.z=Math.PI/2.5;tail.position.set(0,.02,-.27)}else if(archetype==='goblin'){tail=null}else if(archetype==='slime'){tail=null}else if(archetype==='penguin'){tail=cone(.12,.28,palette.body,8);tail.rotation.x=-Math.PI/2;tail.position.set(0,-.02,-.26)}if(tail){parts.pelvis.add(tail);parts.tail=tail}}
function addArchetypeDetails(parts,archetype,palette){if(archetype==='raccoon'){earPair(parts,palette.body,{round:true});face(parts,palette,{snout:true,mask:true});addTail(parts,archetype,palette)}else if(archetype==='cat'){earPair(parts,palette.body);face(parts,palette,{snout:true});addTail(parts,archetype,palette)}else if(archetype==='capybara'){earPair(parts,palette.body,{round:true});face(parts,palette,{snout:true})}else if(archetype==='penguin'){face(parts,palette,{beak:true});addTail(parts,archetype,palette)}else if(archetype==='frog'){for(const side of[-1,1]){const e=sph(.12,palette.body,10,7);e.position.set(side*.16,.22,.07);parts.head.add(e)}face(parts,palette,{})}else if(archetype==='goblin'){earPair(parts,palette.skin,{long:true});face(parts,palette,{snout:true})}else if(archetype==='slime'){parts.head.scale.set(1.12,.86,1.02);face(parts,palette,{})}else if(archetype==='fruit'){const leaf=cone(.13,.42,palette.accent,7);leaf.rotation.z=-.5;leaf.position.set(.06,.34,0);parts.head.add(leaf);face(parts,palette,{})}else if(archetype==='robot'){const antenna=cyl(.025,.025,.26,palette.accent,8);antenna.position.set(0,.31,0);const tip=sph(.055,0xffdd65,8,6);tip.position.set(0,.46,0);parts.head.add(antenna,tip);face(parts,palette,{})}else face(parts,palette,{})}
function createCharacter({archetype='raccoon',scale=1,palette={},shirt=null,pants=null,headScale=1,bodyScale=1,limbScale=1}={}){const THREE=T();if(!THREE)return null;const base=defaults(archetype),p={...base,...palette};const root=group(),rig=group(),pelvis=group(),torso=group(),headPivot=group(),head=group(),leftArm=group(),rightArm=group(),leftLeg=group(),rightLeg=group();root.add(rig);rig.add(pelvis);pelvis.add(torso,leftLeg,rightLeg);torso.add(headPivot,leftArm,rightArm);headPivot.add(head);const bodyColor=shirt??p.body,legColor=pants??p.accent;const torsoMesh=pos(box(.66*bodyScale,.74*bodyScale,.40*bodyScale,bodyColor),0,.42,0);const belly=pos(box(.43*bodyScale,.42*bodyScale,.025,p.belly),0,.38,.214*bodyScale);torso.add(torsoMesh,belly);const headMesh=sph(.34*headScale,p.skin||p.body,16,10);headMesh.scale.y=.94;head.add(headMesh);headPivot.position.set(0,.99*bodyScale,0);for(const [arm,side] of [[leftArm,-1],[rightArm,1]]){arm.position.set(side*.42*bodyScale,.72*bodyScale,0);const upper=pos(cyl(.105,.12,.58*limbScale,p.skin||p.body,10),0,-.27*limbScale,0);arm.add(upper);arm.userData.mesh=upper}for(const [leg,side] of [[leftLeg,-1],[rightLeg,1]]){leg.position.set(side*.19*bodyScale,.03,0);const lower=pos(cyl(.12,.145,.64*limbScale,legColor,10),0,-.29*limbScale,0);const foot=pos(box(.22,.13,.32,p.accent),0,-.61*limbScale,.08);leg.add(lower,foot);leg.userData.foot=foot}const parts={root,rig,pelvis,torso,headPivot,head,leftArm,rightArm,leftLeg,rightLeg,torsoMesh};addArchetypeDetails(parts,archetype,p);root.scale.setScalar(scale);root.userData.gutpopperCharacter={version:VERSION,archetype};return controller(parts,{archetype,palette:p})}
function controller(parts,{archetype,palette}){let state='idle',speed=1,time=0,reaction=null,baseY=0;const root=parts.root;function setState(next,opts={}){state=next||'idle';speed=Number(opts.speed||1);return api}function resetPose(){parts.torso.rotation.set(0,0,0);parts.headPivot.rotation.set(0,0,0);parts.leftArm.rotation.set(0,0,0);parts.rightArm.rotation.set(0,0,0);parts.leftLeg.rotation.set(0,0,0);parts.rightLeg.rotation.set(0,0,0);parts.rig.position.y=baseY;parts.rig.rotation.set(0,0,0);parts.rig.scale.set(1,1,1)}function animateState(t){const s=Math.sin(t),c=Math.cos(t);if(state==='walk'||state==='run'){const amp=state==='run'?.72:.48,freq=(state==='run'?8:5.2)*speed,a=Math.sin(time*freq)*amp;parts.leftArm.rotation.x=-a;parts.rightArm.rotation.x=a;parts.leftLeg.rotation.x=a;parts.rightLeg.rotation.x=-a;parts.rig.position.y=baseY+Math.abs(Math.sin(time*freq))* (state==='run'?.07:.035);parts.torso.rotation.z=Math.sin(time*freq*.5)*.035}else if(state==='carry'){parts.leftArm.rotation.x=-1.0;parts.rightArm.rotation.x=-1.0;parts.leftArm.rotation.z=-.10;parts.rightArm.rotation.z=.10;parts.rig.position.y=baseY+Math.sin(time*4)*.025}else if(state==='work'){parts.leftArm.rotation.x=-.75+Math.sin(time*7)*.45;parts.rightArm.rotation.x=-.35;parts.torso.rotation.z=Math.sin(time*3.5)*.045}else if(state==='jump'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftLeg.rotation.x=.38;parts.rightLeg.rotation.x=.38;parts.rig.position.y=baseY+.10}else if(state==='celebrate'){parts.leftArm.rotation.x=-2.35;parts.rightArm.rotation.x=-2.35;parts.leftArm.rotation.z=-.25;parts.rightArm.rotation.z=.25;parts.rig.position.y=baseY+Math.abs(Math.sin(time*7))*.14;parts.headPivot.rotation.z=Math.sin(time*4)*.08}else if(state==='fail'){parts.torso.rotation.x=.22;parts.headPivot.rotation.x=.35;parts.leftArm.rotation.x=.35;parts.rightArm.rotation.x=.35;parts.rig.position.y=baseY-.08}else if(state==='wave'){parts.rightArm.rotation.x=-1.45;parts.rightArm.rotation.z=.45+Math.sin(time*8)*.35;parts.leftArm.rotation.x=-.15;parts.rig.position.y=baseY+Math.sin(time*3)*.025}else{parts.rig.position.y=baseY+Math.sin(time*2.25)*.025;parts.headPivot.rotation.y=Math.sin(time*.8)*.07;parts.leftArm.rotation.z=-.05+Math.sin(time*2)*.025;parts.rightArm.rotation.z=.05-Math.sin(time*2)*.025}if(archetype==='slime'){const squish=1+Math.sin(time*2.4)*.025;parts.rig.scale.set(1+(1-squish)*.6,squish,1+(1-squish)*.6)}}function react(kind='hit',{duration=.22,strength=1}={}){reaction={kind,start:time,duration:Math.max(.08,duration),strength};if(kind==='squash')window.GutpopperShaderFX?.three?.screenPulse?.({duration:90});return api}function animateReaction(){if(!reaction)return;const p=Math.min(1,(time-reaction.start)/reaction.duration),w=Math.sin(p*Math.PI)*reaction.strength;if(reaction.kind==='hit'){parts.rig.rotation.z=Math.sin(p*Math.PI*4)*.10*w;parts.rig.scale.set(1+w*.08,1-w*.12,1+w*.08)}else if(reaction.kind==='squash'){parts.rig.scale.set(1+w*.14,1-w*.18,1+w*.14)}else if(reaction.kind==='pop'){parts.rig.scale.set(1+w*.16,1+w*.16,1+w*.16)}if(p>=1){reaction=null;parts.rig.scale.set(1,1,1);parts.rig.rotation.z=0}}function update(dt=1/60,t=null){time=t==null?time+Math.min(.05,Math.max(0,dt)):Number(t)||0;resetPose();animateState(time);animateReaction();return api}function attach(partName,obj,{x=0,y=0,z=0,rx=0,ry=0,rz=0,scale=1}={}){const part=parts[partName];if(!part||!obj)return null;part.add(obj);obj.position.set(x,y,z);obj.rotation.set(rx,ry,rz);obj.scale.setScalar?.(scale);return obj}function lookAtXZ(target,{amount=.45}={}){if(!target||!root?.parent)return;const wp=root.getWorldPosition(new (T()).Vector3()),dx=target.x-wp.x,dz=target.z-wp.z;root.rotation.y=Math.atan2(dx,dz)*amount;return api}const api={root,parts,archetype,palette,setState,update,react,attach,lookAtXZ,get state(){return state}};return api}
function preset(name,opts={}){const map={raccoon:{archetype:'raccoon'},cat:{archetype:'cat'},capybara:{archetype:'capybara'},penguin:{archetype:'penguin'},frog:{archetype:'frog'},goblin:{archetype:'goblin'},slime:{archetype:'slime',bodyScale:1.08,headScale:1.05,limbScale:.82},fruit:{archetype:'fruit',bodyScale:.94,headScale:1.08},robot:{archetype:'robot'},worker:{archetype:'humanoid'}};return createCharacter({...map[name],...opts})}
window.GutpopperCharacters={version:VERSION,create:createCharacter,preset,states:['idle','walk','run','jump','carry','work','celebrate','fail','wave'],reactions:['hit','squash','pop']};window.dispatchEvent(new CustomEvent('gutpopper-character-forge-ready',{detail:{version:VERSION}}));})();\n`}

export async function writeCharacterRigKit({gameDir}){const starter=path.join(gameDir,'starter');await fsp.mkdir(starter,{recursive:true});const manifest={name:'Gutpopper Character + Rig + Animation Forge',version:CHARACTER_FORGE_VERSION,presets:['raccoon','cat','capybara','penguin','frog','goblin','slime','fruit','robot','worker'],states:['idle','walk','run','jump','carry','work','celebrate','fail','wave'],reactions:['hit','squash','pop'],principle:'Reuse a common lightweight procedural rig and authored motion language; customize silhouette, palette, proportions and accessories per game.'};await Promise.all([fsp.writeFile(path.join(starter,'character-forge.js'),runtime(),'utf8'),fsp.writeFile(path.join(starter,'character-forge.json'),JSON.stringify(manifest,null,2),'utf8')]);return manifest}

export function characterForgeBrief(){return `GUTPOPPER CHARACTER + RIG + ANIMATION FORGE v${CHARACTER_FORGE_VERSION}\n- ./starter/character-forge.js exposes window.GutpopperCharacters for Three.js projects.\n- Presets: raccoon, cat, capybara, penguin, frog, goblin, slime, fruit, robot and worker. Treat them as kitbashable production bases, not final identical characters.\n- Each character returns a lightweight hierarchical rig with root/pelvis/torso/head/arms/legs parts plus a controller: setState(), update(), react(), attach() and lookAtXZ().\n- Standard authored motion states: idle, walk, run, jump, carry, work, celebrate, fail and wave. Reactions: hit, squash and pop.\n- Customize palette, proportions, clothing colors and accessories; attach Model/Vector Forge props to hands/head/body instead of rebuilding animation plumbing.\n- Prefer these rigs for small stylized Poki mascots and workers where procedural animation is sufficient. Important hero characters that need complex deformation/facial animation can later graduate to GLB + skeletal animation without changing the surrounding game systems.\n- Do not leave characters static. Core mascots should visibly idle and react; locomotion/work/reward/fail beats should use a meaningful state.\n- Keep animation code-driven and lightweight so dozens of background characters remain cheap.`}
